/*
 * AddStringForm Messages
 *
 * This contains all the text for the AddStringForm container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AddStringForm';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AddStringForm container!',
  },
});
