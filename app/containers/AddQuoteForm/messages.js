/*
 * AddQuoteForm Messages
 *
 * This contains all the text for the AddQuoteForm container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AddQuoteForm';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AddQuoteForm container!',
  },
});
