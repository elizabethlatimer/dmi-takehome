// import { selectAddQuoteFormDomain } from '../selectors';

describe('selectAddQuoteFormDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
